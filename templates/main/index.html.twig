{% extends 'base_main.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}

<div class="main-about">
    <div class="main-container">
        <h1>Bienvenue sur les recettes AMIGO 👨🏻‍🍳</h1>
        <h2>Site créé par la Légion 42</h2>
        <h3>N'hésitez pas à vous inscrire et à proposé vos recettes</h3>
    </div>
</div>

<div class="main-rct-top">
    <div class="main-rct-container">
        <h2>Une recette au hasard</h2>

                    <h2>{{ recette.title }}</h2>
                    <img src="{{ asset('image/'~recette.image) }}" alt="{{ recette.title }}">
                    <a href="{{ path('app_recettes_show', {'id': recette.id}) }}">En savoir plus</a>
                    <a href="{{ path('app_recettes_like', {'id': recette.id}) }}" class="js-like">
                        <span class="js-likes"> {{recette.likes | length}} </span>
                    {% if app.user and recette.isLikedByUser(app.user) %}
                            <i class="fa-solid fa-heart"></i>
                    {% else %}
                            <i class="fa-regular fa-heart"></i>
                    {% endif %}
                    </a>
                </div>
            </div>

    </div>
</div>

{% endblock %}
